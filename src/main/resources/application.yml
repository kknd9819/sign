server:
  port: 10011

spring:
  jpa:
    database: MYSQL
    show-sql: true
#    properties:
#      hibernate.format_sql: true
    hibernate:
      ddl-auto: none

  datasource:
    url: jdbc:mysql://172.16.0.15:3306/sign?characterEncoding=utf8
    username: root
    password: Zheng1234

  thymeleaf:
    cache: false
    mode: HTML5
    encoding: UTF-8
    content-type: text/html; charset=utf-8

  http:
    multipart:
      max-file-size: 64096MB
      max-request-size: 64096MB

logging:
  file: sign.log